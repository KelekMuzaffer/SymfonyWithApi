{% extends('base.html.twig') %}

{#Page pour l'inscription d'un user#}

{% block body %}


{{ form_start(formRegister, {'action': path('InscriptionUser'), 'method': 'POST'}) }}

    {%  if error is defined %}
    {{ error }}
    {% endif %}

    {{ form_widget(formRegister)}}

{{ form_end(formRegister) }}

{% endblock %}